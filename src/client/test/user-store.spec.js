import React from 'react';
import { expect } from 'chai';

import UserStore from '../js/stores/user-store';

describe('UserStore', () => {
  it('triggers the appropriate login popup for each provider', () => {
    expect(true).to.be.true;
  });

  it('stores the user object when logged in', () => {
    expect(true).to.be.true;
  });

});
